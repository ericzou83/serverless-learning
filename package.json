{
  "name": "serverless-learning",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "deploy": "serverless deploy",
    "dev": "nodemon --watch 'src/**' --watch '**/serverless.yml' --watch '**/functions.yml' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'serverless offline'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "dotenv": "^16.4.7",
    "dynamoose": "^4.0.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/dotenv": "^6.1.1",
    "nodemon": "^3.1.9",
    "serverless-offline": "^14.4.0",
    "typescript": "^5.8.2"
  }
}